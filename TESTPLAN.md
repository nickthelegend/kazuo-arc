# TESTPLAN — Kazuo (Arc) · end-to-end verification

> Every item has an ID, what it exercises, and the **exact** result that counts as correct. Status is one of
> **PASS** · **FAIL** (with the fix) · **UNTESTED** (with the missing real dependency) · **NOT RUN**.
> Browser items run in **Claude in Chrome** against the deployed product; console and network are checked on
> every browser item — any console error or failed request (outside an item that deliberately creates one)
> fails it.

## 0. Test environment (recorded, not assumed)

| | |
|---|---|
| Landing | https://kazuo-arc.vercel.app (production) |
| Job board | https://kazuo-arc-app.vercel.app (production) |
| Railway broker | `https://broker-production-03b2.up.railway.app` — **not serving**: answers "Application not found"; the service needs `KAZUO_OPERATOR_KEY`, which the session may not push to an external service |
| Broker under test | The same broker build, run locally with the real `.env` (real operator key, real Arc testnet, real SQLite `data/kazuo.db`), exposed through a Cloudflare tunnel so the deployed frontends reach it — since the 15:34 reboot `https://surround-ports-prime-audience.trycloudflare.com`. The key never leaves this machine. |
| Frontends under test | Vercel deployments of the same commits, with `NEXT_PUBLIC_KAZUO_BROKER_URL` pointed at the tunnel (verified in the served JS) |
| Provider | `kazuo start` from this repo, `KAZUO_HOME=~/.kazuo-arc-demo/provider`, key from `.env` via `KAZUO_PRIVATE_KEY`, payout = demo provider `0xff21…489B`, adapters echo + codex (`KAZUO_SANDBOX=none`, documented) |
| Buyer (CLI/MCP) | demo payer `0x0329…9F36` (16.3 USDC on Arc testnet) |
| Chain | Arc testnet `eip155:5042002`, USDC `0x3600…0000`, KazuoLog `0x383f5153…65eef3`; World Chain mainnet AgentBook `0xA23aB271…b944dA` |

## 1. Landing — kazuo-arc.vercel.app

| ID | What | Correct means | Status |
|---|---|---|---|
| L1 | Page load | HTTP 200; `<title>` = "Kazuo — rent out your idle AI subscription, get paid in USDC"; zero console errors; every same-origin request 2xx/3xx | **PASS** — 13:57: title exact; 0 console errors; all same-origin requests 200 (22) |
| L2 | Hero content + CTAs | Headline "Your AI subscription is idle most of the day"; pill "Live on Arc testnet"; sponsor row links Arc → `circle.com/arc`, World AgentKit → `docs.world.org/agents/agent-kit/integrate`, Privy → `privy.io`; "Post a job" href = `https://kazuo-arc-app.vercel.app` | **PASS** — 13:57: headline + "Live on Arc testnet" pill exact; sponsor links Arc→circle.com/arc, World AgentKit→docs.world.org/agents/agent-kit/integrate, Privy→privy.io; "Post a job" ×2 and "Open app" → https://kazuo-arc-app.vercel.app. Note outside the definition: "Start earning" → GitHub repo, which 404s until the repo is public (PLAN 7.7) |
| L3 | Navbar anchors | Each of How it works / The network / Earn / Adapters / Receipts / Security / FAQ targets an element with the matching id (`#how #bento #earn #adapters #ledger #security #faq`), and clicking scrolls it into view | **PASS** — 14:52: all 7 header links (How it works, The network, Earn, Adapters, Receipts, Security, FAQ) set the matching hash and bring their section to top 0. Method: Chrome reports the tab `visibilityState: hidden`, where the page's CSS `scroll-behavior: smooth` animation is throttled to a standstill (30–40 s without moving), so the check ran with `scroll-behavior: auto` set on the page for the test; the smooth animation itself is browser-native CSS. 0 console errors |
| L4 | Receipts section, live | Requests `GET <broker>/api/receipts` and `GET <broker>/api/network` both 200; section shows real receipt rows (job id, payer → provider, amount) read from KazuoLog, or its explicit empty state if the log has none; contract link = ArcScan `0x383f…65eef3` | **PASS** — 15:48: `/api/receipts` and `/api/network` both 200 on reload; 6 real receipt rows from KazuoLog (job id, payer → provider, µUSDC amount), newest first, including `job_dWgs8hE2r0M_` published minutes earlier; links to ArcScan contract `0x383f…65eef3`, the USDC token and each settlement tx; 0 console errors |
| L5 | Receipts section, broker unreachable | Section renders its offline/empty state instead of crashing; no uncaught JS exception (the failed fetch itself is the condition under test) | **PASS** — 13:56, broker down behind the tunnel (`/api/receipts` + `/api/network` → 503): section shows "live feed offline — the contract above is still readable on ArcScan" with both ArcScan links; 0 console errors/exceptions; all 11 same-origin requests 200 |
| L6 | FAQ | Each question toggles its answer open and closed | **PASS** — 14:56, real mouse clicks at the button's screen position: clicking question 3 opened it and closed question 1 (single-open accordion; state `false,false,true,…`, answer rendered); clicking it again closed it (all `aria-expanded` false); 0 console errors. Two earlier attempts missed only because the hidden tab stalls smooth scrolling / the collapse animation, so the click landed off the moving button; the passing run waited for layout to be stable for 1 s. Note: the closing *animation* itself is rAF-driven and paused in a hidden tab — the state closes immediately |
| L7 | Copy + footer | No "Xorv", "Hedera", "HBAR" or "HashScan" anywhere in rendered text; footer Build column includes World AgentKit and Privy links; footer shows `eip155:5042002` | **PASS** — 13:57: 0 matches for xorv|hedera|hbar|hashscan in rendered text; footer Build column has World AgentKit + Privy; footer shows eip155:5042002 |
| L8 | Mobile (375px) | No horizontal scroll; the nav menu opens and its links work | **PASS** — 16:15 at 375×812 with mobile emulation (in the desktop app's Browser pane: the Chrome window here is maximized and ignores resize, `innerWidth` stayed 1920): page `scrollWidth` 375, the only element wider than the viewport is the comparison table inside its own `overflow-x` container; "Open menu" opens the menu (`aria-label` → "Close menu") listing How it works / The network / Earn / Adapters / Receipts / Security / FAQ / GitHub / Open app; tapping Receipts sets `#ledger`, closes the menu and brings the section to top 0; 0 console errors |
| L9 | Section content | Each section renders its heading and its content: `#how` (the numbered steps), `#bento` (the network facts), `#earn` (the earning flow), `#adapters` (Claude Code, Codex, Grok, OpenCode, OpenAI-compatible named), `#ledger` (contract, USDC, network rows), `#security` (the sandbox story), `#faq` (questions), CTA; no section empty, no "undefined"/"NaN"/"[object Object]" text | NOT RUN |
| L10 | Outbound links | Every distinct external `href` on the page answers HTTP < 400 when fetched | NOT RUN |
| L11 | Unknown route | `/this-page-does-not-exist` → HTTP 404 with a not-found page; no uncaught exception | NOT RUN |

## 2. Job board — kazuo-arc-app.vercel.app

| ID | What | Correct means | Status |
|---|---|---|---|
| A1 | Home load with live broker | 200; composer, "Recent jobs", "Live providers" render; sidebar footer shows "N provider(s) live" with N = broker `stats.providersLive` and `eip155:5042002`; `/api/network`, `/api/jobs`, `/api/providers` 200; zero console errors | **PASS** — 14:59: composer (placeholder "Tell the network what to build…"), "Recent jobs" (5 persisted jobs), "Live providers" (kazuo-proof-node, Online, Echo + Codex, address, beat 8 s ago); sidebar footer "1 provider(s) live · eip155:5042002" matching broker `stats.providersLive` = 1; header "Sign in" (Privy); `/api/network`, `/api/jobs?limit=15`, `/api/providers` all 200 across 10 polls; 0 console errors |
| A2 | Empty states | With zero providers: "No providers online" + install hint. With zero jobs: "No jobs yet" + "settles on Arc in about a second" | **PASS (providers-empty)** — 16:12, provider stopped, broker up: "Live providers" shows "No providers online" + "Run npm i -g @kazuo/cli && kazuo init on any machine with Claude Code, Codex or Grok installed."; sidebar "0 provider(s) live"; broker calls 200; 0 console errors. (The hint names the npm package that is not yet published — PLAN 7.6.) **jobs-empty is UNTESTED on this broker**: producing it would mean deleting the 5 real persisted jobs in `data/kazuo.db`, which is not an acceptable way to test a UI state |
| A3 | Broker offline | Sidebar "broker offline" (red dot); lists show "Can't reach the broker"; no uncaught exception | **PASS** — 16:10, broker and provider stopped: sidebar footer "broker offline" with a red dot and `eip155:5042002`; "Recent jobs" and "Live providers" both show "Can't reach the broker", providers adds "Start it with pnpm broker in the kazuo repo."; the polls' 503s through the tunnel are the condition under test; console has no errors or exceptions (only the wallet-detection debug line) |
| A4 | Composer validation | Empty prompt → no `/api/quotes` request. Max price `0` or `abc` → message "Set a maximum price above zero." and no `/api/quotes` request | **PASS** — 15:40, real typing and clicks: empty prompt → "Get a quote" disabled, no request; max `0` → "Set a maximum price above zero." shown, 0 `/api/quotes` requests; max `abc` → same message, 0 requests; 0 console errors. (Typing only registers after a real click into the field; focusing by script leaves keystrokes undelivered in the hidden tab — a test-harness detail, not an app defect) |
| A5 | Quote, no providers | Broker 503; UI shows "no providers are online right now — start one with `kazuo start`" | **PASS** — 16:14, provider stopped: `POST /api/quotes` 503; composer shows "no providers are online right now — start one with `kazuo start`"; stays on `/`; 0 console errors. (First attempt typed before the freshly navigated page accepted input, so no request was sent; re-run after the page settled) |
| A6 | Quote, provider live | Quote card shows the provider's label, its capability, the price (e.g. $0.0010 for echo), and "goes to" the provider's own address; request body carries the chosen adapter | **PASS** — 15:42: `POST /api/quotes` 200; body `{adapter:"echo", maxPriceUsdMicros:500000}`; card shows kazuo-proof-node, "Echo (test) · 0 done", "pays → 0xff212ecb…489B" (the provider's own payout address), $0.0010, the no-gas note, "Pay $0.0010 and run" and "Cancel — nothing has been paid"; 0 console errors |
| A7 | Quote under every price | Max below the cheapest capability → 503 "no online provider matches that request under $X" shown | **PASS** — 16:07: max `0.0005` (cheapest live capability is $0.0010) → `POST /api/quotes` 503; composer shows "no online provider matches that request under $0.0005"; no navigation; 0 console errors |
| A8 | Model picker | Opens, lists the adapters, the selection is reflected in the quote request's `adapter` | **PASS** — 15:42: opens on click; lists Any model (cheapest, checked), Claude Code, Codex, Grok, OpenCode, OpenAI-compatible (local), Echo (test); choosing Echo relabels the picker "Echo" and the next quote request carries `adapter:"echo"` |
| A9 | Pay with no wallet (`/api/pay`) | POST `/api/pay` 200 `{jobId, transaction}`; browser lands on `/jobs/<id>`; job reaches `completed` with the echo result; payment panel shows payer `0x0329…`, paid to provider, amount, network, a working ArcScan link | **UNTESTED** — needs `KAZUO_DEMO_PAYER_KEY` on the Vercel project `kazuo-arc-app`. The key exists only in the local `.env`, and pushing a private key to an external service was refused by the session's permission policy (PLAN 5.5, user action). With it absent the route correctly answers 501 (A10 PASS). The same payment path — quote → EIP-3009 authorization → facilitator settle → job → receipt — is proven for real by P2, P3, P4, P5 and C1/C2 |
| A10 | Pay with no demo key configured | `/api/pay` 501; composer shows "No demo payer configured. Set KAZUO_DEMO_PAYER_KEY…" and stays on the quote | **PASS** — 15:44: clicking "Pay $0.0010 and run" → `POST /api/pay` 501 (the deliberate condition); message "No demo payer configured. Set KAZUO_DEMO_PAYER_KEY in .env.local — see .env.example." shown under the quote; still on `/` with the quote card intact; 0 console errors |
| A11 | Privy sign-in modal | "Sign in" opens the Privy modal with email and wallet options; `auth.privy.io` app-config request 200 (valid app id); closing it returns to "Sign in" with no error text | **PASS** — 16:09: "Sign in" opens `#privy-modal-content` "Sign in to Kazuo — Pay per AI job in USDC on Arc. No gas, no extension needed." with an email field (your@email.com / Submit) and "Continue with a wallet", "Protected by privy"; the page's own resource timing shows Privy's app config `auth.privy.io/api/v1/apps/<app id>` fetched with status 200 and the embedded-wallets iframe loaded (the extension's network list doesn't capture them, so resource timing is the evidence); the × button closes it, header back to "Sign in", no error text; 0 console errors. Escape does not close Privy's modal — Privy's own behaviour, not configurable from the app |
| A12 | Pay from a Privy embedded wallet | Signed-in email user with USDC pays; job page reached; ArcScan shows transfer from the embedded wallet | **UNTESTED** — needs a Privy email login, which sends a one-time code to a real inbox the session cannot read, and then testnet USDC sent to the new embedded wallet. The modal and Privy app config are verified live (A11); the wallet-side signing code is covered by `apps/app/test/wallet.test.ts` (T2) |
| A13 | Send USDC from the wallet popover | Transfer tx hash shown, links to ArcScan, balance updates | **UNTESTED** — same dependency as A12 (a signed-in Privy wallet holding USDC) |
| A14 | Completed job page | Result rendered; execution log lists events; provider panel shows label, address, price, took, human-backed rows; receipt panel shows payer / paid to / amount / network, "View transfer on ArcScan" → the settlement tx, "View on-chain receipt" once published, `sha256 <hash>` | **PASS** — 15:46 on `/jobs/job_dWgs8hE2r0M_` (the P2 job): echo result rendered; execution log 4 events + final output; provider panel kazuo-proof-node, 0xff21…489B ↗, price $0.0010, took 697 ms, events 5, provider/buyer human-backed "not proven" (correct — neither address is verified); receipt panel payer 0x0329…9F36, paid to 0xff21…489B, 1000 µUSDC, eip155:5042002; "View transfer on ArcScan" → tx 0x12c3a36b… (C1), "View on-chain receipt" → tx 0x093bb763… (C2); `sha256 c554fc1f…f8b594` matches the on-chain receipt; 0 console errors |
| A15 | Unknown job id | `/jobs/job_doesnotexist` renders without a 500 and without an uncaught exception, and says the job wasn't found | **PASS** — 15:47: renders "Job not found — It may have expired, or the broker restarted."; 0 console errors or exceptions. The network panel also listed `HEAD /jobs/job_doesnotexist 503` ×2 that the app never sends: the page's own `fetch(…, {method:"HEAD"})` returns 200, and curl HEAD returns 200 under every header variant tried (plain, navigate, prefetch, prerender, RSC, router-prefetch, extension origin). They appear only right after automation navigations, so they are the harness's own probes, not app traffic |
| A16 | Providers page | Every live provider with its capabilities, cheapest price, address, heartbeat age; "human" badge only when `humanBacked` is true | **PASS** — 15:00: "Providers" page lists kazuo-proof-node (Online, Echo (test) · Codex, cheapest $0.0010, 0xff21…489B, ap-south, beat 9 s ago, 0 done); no "human" badge, correct since the node is not verified; `/api/providers` 200; 0 console errors |
| A17 | Network page | network `eip155:5042002`; fee payer = operator `0xeEE4…B51E`; USDC link to ArcScan token; audit counts; receipts list read from chain | **FAIL** (15:00) — facts correct (network eip155:5042002, facilitator self-hosted, fee payer 0xeEE4…B51E, USDC → ArcScan token, providers live 1, jobs settled 3, paid $0.6520, KazuoLog link), but "Receipts from the chain" shows "No receipts yet" while KazuoLog holds 65 entries. Root cause and fix: see the log-index entries below; re-test after broker restart. **Re-run 15:47 → PASS:** same facts (now jobs settled 4, paid $0.6530), audit counts shown, and "Receipts from the chain" lists 6 real receipts read from KazuoLog (job ids, payer → provider, amounts) with the contract link; 0 console errors |
| A18 | Expired / unknown quote at pay time | Paying a quote id the broker doesn't know → error "quote not found or expired — request a new one" shown; no navigation | **Broker behaviour PASS (B5); in-browser display UNTESTED** — the broker returns 404 with exactly that message for an unknown or expired quote (B5, 16:14). Reaching it from the job board needs a browser that can pay, and without `KAZUO_DEMO_PAYER_KEY` on Vercel the pay route stops at 501 first (A10), same dependency as A9/A12 |
| A19 | Pay the same quote twice | Second attempt → 409 "this quote has already been paid" shown | **Broker behaviour PASS; in-browser display UNTESTED.** 16:18: a real echo job paid with `kazuo run` (quote `qte_1Gl6GEYrhdxT`, job `job_J386WaMK0o0e`, settlement `0xbcf23234…0d8b`), then `POST /api/jobs/qte_1Gl6GEYrhdxT` again — both with no payment and with a payment header — → 409 `{"error":"this quote has already been paid","jobId":"job_J386WaMK0o0e"}`. Showing that message in the job board needs a browser that can pay, which needs either `KAZUO_DEMO_PAYER_KEY` on Vercel (a private key the session may not push; user action) or a Privy email login with an OTP sent to a real inbox |
| A20 | Mobile (375px) | Drawer opens from the hamburger, closes on route change; no horizontal scroll | **FAIL → fixed → PASS.** 16:05 FAIL: at 375 px "Recent jobs" was 537 px wide and the layout viewport grew to 557 px (horizontal scroll; the drawer tap on Providers missed because of it) — root cause and fix in the 16:08 log entry. 16:15 re-run on production `dpl_H2YrccYnu3gm32C1LPkcCEG2LCcW`, same 375×812 emulation, 8 job rows rendered: `innerWidth` = `clientWidth` = `scrollWidth` = 375, no element wider than the viewport; hamburger opens the drawer ("Close navigation"; Jobs / Providers / Network); tapping Providers routes to `/providers` and the drawer closes ("Open navigation"), still 375 wide; that page's own resource timing: 8 broker calls all 200, 0 failures |
| A21 | Unknown route | `/this-page-does-not-exist` → HTTP 404 with a not-found page; no uncaught exception | NOT RUN |
| A22 | Live job stream | Job page opened while a real Codex job is running shows a non-terminal status, connects `GET /api/jobs/<id>/stream` (200, `text/event-stream`), and without a reload turns `Completed` with the model's answer and the receipt panel | NOT RUN |
| A23 | Broker drops and returns mid-session | With the job board open: broker stopped → sidebar "broker offline" and "Can't reach the broker" within one poll; broker restarted → sidebar back to "1 provider(s) live" and lists repopulate **without a reload**; no uncaught exception | NOT RUN |
| A24 | Job page while the broker is down | `/jobs/<a real id>` with the broker stopped renders (HTTP 200, no 500) with a not-found/unreachable message; no uncaught exception | NOT RUN |

## 3. Broker API (exercised from the browser)

| ID | What | Correct means | Status |
|---|---|---|---|
| B1 | `GET /health` | 200 `{"ok":true,"at":<ms>}` | **PASS** — 15:45, loaded in the browser: `{"ok":true,"at":1789291169717}` |
| B2 | `GET /api/network` | `network` `eip155:5042002`; `operator.address` `0xeEE4CA97…B51E`; `usdc` `0x3600…0000`; `log.address` `0x383f…65eef3` | **PASS** — 15:45 (browser): all four exact. Found alongside: `logLastError` was a heartbeat publish refused with "rate limit exceeded" and exposed viem's full request dump — see the 15:50 RPC-starvation fix |
| B3 | `GET /api/providers` | Array; each has `humanBacked` boolean; **no** `token` or `humanId` field | **PASS** — 15:45 (browser): `{providers:[…]}` with 1 node; `humanBacked:false` (boolean), `humanProof:null`; field list has no `token` and no `humanId` |
| B4 | `POST /api/quotes` validation | no prompt → 400 "prompt is required"; `maxPriceUsdMicros: 0` → 400; 20,001-char prompt → 400 "prompt is too long" | **PASS** — 15:45 (curl through the public tunnel): 400 "prompt is required"; 400 "maxPriceUsdMicros must be a positive number"; 400 "prompt is too long (max 20k chars)" |
| B5 | `POST /api/jobs/<unknown>` | 404 "quote not found or expired — request a new one" | **PASS** — 15:45: 404 with exactly that message |
| B6 | `POST /api/jobs/<quote>` unpaid | 402; `payment-required` header decodes to `accepts[0]` with `scheme:"exact"`, `network:"eip155:5042002"`, `payTo` = quoted provider address, `amount` = quote `usdcAmount`, `asset` = USDC, `extra` = `{name:"USDC",version:"2"}` | **PASS** — 15:45: fresh echo quote `qte_b4RNQILid5dj` → 402; header decodes to `{scheme:"exact", network:"eip155:5042002", amount:"1000", asset:"0x3600…0000", payTo:"0xff212ecb…489B", maxTimeoutSeconds:300, extra:{name:"USDC",version:"2"}}` — payTo = the quoted provider, amount = the quote's 1000 µUSDC |
| B7 | `GET /api/agentkit/challenge` | `for=register`: `info.domain` = broker hostname, `info.uri` ends `/api/providers/register`, 32-hex nonce, expiry ≈ issuedAt + 5 min, `supportedChains` = `eip155:480`/`eip191`. `for=quote`: uri ends `/api/quotes` | **PASS** — 15:47: response is `{agentkit:{info,supportedChains,schema}, header:"agentkit"}`; domain = tunnel hostname; uri `…/api/providers/register`; nonce `5cf25301…b7a0` (32 hex); issuedAt 09:19:24.548Z → expiration 09:24:24.548Z (exactly 5 min); supportedChains `[{chainId:"eip155:480", type:"eip191"}]`; `for=quote` uri `…/api/quotes` |
| B8 | Quote with a garbage `agentkit` header | 401 with an agentkit error message; no quote created | **PASS** — 15:45: 401 "Invalid agentkit header: not valid base64"; `kazuo_quotes_total` did not move for it |
| B9 | `GET /metrics` | 200 text; contains `# TYPE kazuo_uptime_seconds gauge` and `kazuo_providers_connected` | **PASS** — 15:45 (browser): both present, plus provider/job/settlement/audit series |
| B10 | `GET /api/receipts` | 200; `log.address` = KazuoLog; entries parsed from chain (no error field) | **PASS** — 15:46 (browser): 200; `log.address` `0x383f…65eef3`; 6 receipts decoded from `Entry` events (seq 66, 58, 57, 54, 48, 26) with full payloads; no `error` field; `sync` `{scannedTo 56,089,361, head 61,876,253, entries 59, lastError null}` |
| B11 | CORS | Response to the app origin carries `Access-Control-Allow-Origin` = that origin and exposes `PAYMENT-REQUIRED` | **PASS** — 15:45: preflight 204 and the 402 both carry `access-control-allow-origin: https://kazuo-arc-app.vercel.app`; expose-headers include `PAYMENT-REQUIRED`/`payment-required` and `PAYMENT-RESPONSE`; allow-headers include `agentkit` and `PAYMENT-SIGNATURE` |
| B12 | `humanBackedOnly` with no human-backed node | 503 "no human-backed provider matches that request under $X" | **PASS** — 15:45: 503 `{"error":"no human-backed provider matches that request under $0.5000","providersLive":1}` |
| B13 | Rate limit | 31st `POST /api/quotes` within a minute from one client → 429 | **PASS** — 16:17, 33 sequential quote requests through the public tunnel: 1–30 → 200, 31–33 → 429 `{"error":"rate limit exceeded — 30 requests per 60s","retryAfterSeconds":51}` (client identified by the proxied address, `KAZUO_TRUST_PROXY=1`) |
| B14 | `GET /api/jobs` | 200 `{jobs:[…]}`, newest first by `createdAt`, each with `id` and `status`; no `token`/`humanId` fields | NOT RUN |
| B15 | `GET /api/jobs/:id` | Known id → 200 `{job}` with `events`, `payment`, `resultHash`; unknown → 404 `{"error":"not found"}` | NOT RUN |
| B16 | `GET /api/jobs/:id/stream` | Completed job → 200 `text/event-stream` emitting `event: snapshot` then `event: done`, then the stream closes; unknown → 404 | NOT RUN |
| B17 | `GET /api/log/:kind` | `registry`, `heartbeat`, `receipts` → 200 `{log, entries, sync}` with entries of that kind only; `bogus` → 404 `unknown log stream "bogus"` | NOT RUN |
| B18 | `POST /api/providers/register` validation | Missing label → 400 "label is required"; bad address → 400 "address must be an EVM address…"; no nodeId → 400 "nodeId is required"; no capabilities → 400 "at least one capability is required"; price 0 → 400 `capability "x" needs a positive priceUsdMicros` | NOT RUN |
| B19 | Heartbeat auth | No token or a wrong token → 401 `{"error":"unauthorized"}` | NOT RUN |
| B20 | Provider callbacks auth | `POST /api/jobs/<id>/events` and `/result` without a provider token → 401 | NOT RUN |
| B21 | `POST /api/jobs/:id/cancel` | Unknown → 404; completed job → 409 "job is already completed"; a running Codex job → 200 `{ok:true, status:"failed", refunded:false}`, the node logs the cancel, the job page shows Failed "cancelled by the buyer" | NOT RUN |
| B22 | `GET /api/worldid/status` validation | Non-address subject → 400 `invalid_subject` | NOT RUN |

## 4. On-chain

| ID | What | Correct means | Status |
|---|---|---|---|
| C1 | Settlement tx of a paid job | Receipt `status: success`; one USDC `Transfer(payer → provider, amount)`; tx `from` = operator (buyer's gas = 0) | **PASS** — 15:46, P2's settlement `0x12c3a36b…6d10` read from Arc RPC: `status success`, block 61,876,008, `from` 0xeEE4…B51E (operator), `to` the USDC contract, exactly one Transfer 0x0329…9F36 → 0xff21…489B value 1000 |
| C2 | Receipt entry | KazuoLog `Entry` with kind 3 in a tx whose payload names the job id and the same `resultHash` the job page shows | **PASS** — 15:46: receipt tx `0x093bb763…acd3` `status success` with one `Entry` kind 3, seq 66, author operator, payload `jobId job_dWgs8hE2r0M_`, `transactionHash` = the C1 settlement, `resultHash c554fc1f…f8b594` = the job page's sha256 |
| C3 | Registration entry | On `kazuo start`, a KazuoLog kind-1 entry for the node (or the recorded publish error if the RPC refuses) | **PASS** — 16:06, after the RPC-starvation fix: the broker restart made the node re-register as `prv_Lrp1iDVzYtSd`, and Arc holds KazuoLog kind 1, seq 67, block 61,877,199, tx `0x530bcd9f…1520`, author = operator, payload `{providerId prv_Lrp1iDVzYtSd, label kazuo-proof-node, address 0xff21…489B, capabilities [echo, codex]}`, followed by heartbeat kind 2 seq 68. `/api/network` went from `registry 0, heartbeat 0` + a rate-limit `logLastError` (before the fix) to `registry 1, heartbeat 1, lastError null` |
| C4 | AgentBook read | Contract `0xA23aB271…b944dA` has bytecode on World Chain; `lookupHuman` returns real values | **PASS** (13:44 — 3,569 bytes; direct `readContract` → 0 for unregistered addresses) |
| C5 | Settlement on World Chain Sepolia | A job settles on `eip155:4801` | **UNTESTED** — facilitator has 0 ETH on World Chain Sepolia; faucets need a human |
| C6 | Reassignment on provider failure | A paid job whose provider fails is re-dispatched to another live provider at no extra charge and completes | NOT RUN |

## 5. Provider, CLI and MCP flows

| ID | What | Correct means | Status |
|---|---|---|---|
| P1 | `kazuo start` registers | Node appears on `/providers` in the browser within one heartbeat, status online, both capabilities listed | **PASS** — 16:16: provider started on the rebuilt CLI with the broker already up; `/providers` in Chrome shows kazuo-proof-node "Online", "Echo (test) · Codex", 0xff21…489B, ap-south, "beat 9s ago", from $0.0010, 3 done; `/api/providers` 200; 0 console errors |
| P2 | `kazuo run` echo job | JSON status completed, echo result, ArcScan settlement link resolves to a successful tx (C1) | **PASS** — 15:43: `kazuo run … --adapter echo --max 0.01 --json` from the demo payer → `status:"completed"`, `jobId job_dWgs8hE2r0M_`, quoted $0.0010 to kazuo-proof-node (`humanBacked:false`), echo result text, `durationMs 3010`, `settlementTransaction 0x12c3a36b…` with its ArcScan link — the tx verified successful in C1 |
| P3 | `kazuo run` real model (codex) | Completed with a model-written answer; settled on chain | **PASS** — 16:02: `kazuo run "Write a Python one-liner that reverses a string. Just the code, nothing else." --adapter codex --max 0.25` → quoted $0.2000 (Codex) to kazuo-proof-node; `status:"completed"`, result `print(input()[::-1])` written by the provider's real Codex CLI, `durationMs 4406`; settlement `0x042613d2…6ddb`; on-chain receipt seq 69 for `job_sbnrVEgq1N2_` (C3 read) |
| P4 | MCP `kazuo_run_job` over stdio | Tool list includes all five `kazuo_*` tools; run returns the result plus "Transaction: https://testnet.arcscan.app/tx/…" | **PASS** — 16:03, built `@kazuo/mcp` over stdio with a real MCP client: tools `kazuo_list_providers, kazuo_network_status, kazuo_quote, kazuo_run_job, kazuo_get_job`; list shows the live node with both prices; quote "Human-backed provider (World ID): no"; `kazuo_run_job` `isError=false` with the echo result, "Paid $0.0010 to kazuo-proof-node", "Transaction: https://testnet.arcscan.app/tx/0x307792e8…9f99" and "On-chain receipt: …/tx/0x2cdcae5c…454a" (KazuoLog seq 70, `job_85cQbrsSkFKD`) |
| P5 | `pnpm m1` zero-gas proof | Script completes and asserts buyer gas = 0 wei | **PASS** — 16:04: verify `isValid:true`; settle `success:true` tx `0xbc95f083…4d12`; provider received 1000 units (expected 1000); payer native drop = payment + gas 0; "payer paid in gas 0 wei — must be 0"; "✔ M1 PASSED — a buyer holding only USDC paid, and never sent a transaction." |
| P6 | `kazuo agentkit status` | Prints "not registered" for the demo provider (matches C4) | **PASS** — 15:41: "looking 0xff212ecb…489B up in AgentBook on World Chain… ✖ not registered — the broker will treat this node as anonymous / register with: kazuo agentkit register" |
| P7 | Provider signs AgentKit proof on register | Broker accepts the proof (no 401) and reports `humanBacked:false` for the unregistered address | **PASS** — 16:16, provider on the rebuilt CLI: broker log "register 0xff212ecb82E3b06c0a2A7a9Ce343e0a1868c489B: agentkit proof verified — no human in AgentBook" then `POST /api/providers/register → 200` (1.49 s, including the live AgentBook read on World Chain); `/api/providers` reports the node `humanBacked:false, humanProof:null`; registration on chain tx `0xcd8c4961…d727`. (Before 16:08 the broker kept no record of whether a proof was sent, so this was not checkable — see log) |
| P8 | CLI surface | `doctor` ends "nothing broken"; `status`, `earnings`, `jobs`, `wallet`, `price`, `logs`, `completion` exit 0 with real data; `test --adapter echo` "every capability works"; `pause` then `resume` both succeed and the node stays online; `config --json` prints no private key | NOT RUN |
| P9 | `/kazuo` skill command | The exact command the skill runs, `kazuo run --json --yes --max 0.30 "<task>"`, through the public broker → `status:"completed"` with a settlement transaction | NOT RUN |
| P10 | `--human-backed-only` from the CLI | With no human-backed node: refuses before paying with "no human-backed provider matches that request under $X"; no settlement | NOT RUN |
| P11 | Provider dies mid-job | Provider process stopped while a paid Codex job runs → the buyer gets a terminal `failed` (no hang), the job page shows Failed, a receipt with `ok:false` is published; the restarted node re-registers and goes back online | NOT RUN |
| N1 | Node's local status server | `localhost:8411/health` → 200 JSON `{ok:true, connected:true, providerId}`; `/info` → 200 JSON with label, address, capabilities and no private key; `/` → 200 status page that shows the node live (not "reconnecting"), 0 console errors | NOT RUN |

## 5b. World ID — Selfie Check (added 14:15 at the user's request, modelled on `_references/comitment-issues`)

| ID | What | Correct means | Status |
|---|---|---|---|
| W1 | `POST /api/worldid/request` | 200; `app_id` = `app_d7d26bb5…89e2`, `rp_context.rp_id` = `rp_30d29cd7ee86bae6`, `action` `kazuo-provider` (provider) or `kazuo-human` (buyer), `signal` = the lowercased address, `preset` `selfieCheckLegacy`, 65-byte signature recovering to signer `0xd4F041AB…9b78Dc`; non-address subject → 400 `invalid_subject` | **PASS** — 16:13–16:16: provider ticket `{app_id app_d7d26bb561fa95de4117df5b5ecc89e2, action kazuo-provider, signal 0xff212ecb…489b (lowercased), preset selfieCheckLegacy, allow_legacy_proofs true, rp_context {rp_id rp_30d29cd7ee86bae6, 32-byte nonce, expires_at − created_at = 300}}`; buyer ticket action `kazuo-human` with the lowercased buyer address; signature is 65 bytes, and rebuilding World's RP message (`computeRpSignatureMessage(nonce, created_at, expires_at, action)`, EIP-191 hash) recovers exactly `0xd4F041ABf448dE7C85f305f44e85596c5c9b78Dc`; `subject: "not-an-address"` → 400 `invalid_subject` |
| W2 | `POST /api/worldid/verify` with a proof for a nonce the broker never minted | 403 `unknown_nonce`, and World is not called | **PASS** — 16:14: a well-formed IDKit result with a nonce the broker never minted → 403 `{"code":"unknown_nonce"}`; the nonce lookup throws before `portalVerify` is reached (`worldid.ts` checks known nonce → expiry → action → signal hash, and only then calls developer.world.org). A body with no nonce/responses → 400 `invalid_result` |
| W3 | `kazuo verify` end to end | Terminal shows the QR and details; after a real World App Selfie Check scan the broker forwards the proof to `developer.world.org/api/v4/verify/rp_30d29cd7ee86bae6`, World returns success, CLI prints "human verified — selfie · <address>", and a row lands in SQLite `human_verifications` | **UNTESTED** — needs a person to scan the QR with World App and complete a Selfie Check; no automation can produce a real World ID proof. Everything around the scan is verified: RP-signed ticket (W1), nonce and replay rejection before World is called (W2), a real bridge request created by `kazuo verify` (14:22 log). To run: `node packages/cli/dist/index.js verify --broker http://localhost:8402 --address 0xff212ecb82E3b06c0a2A7a9Ce343e0a1868c489B` and scan |
| W4 | `GET /api/worldid/status` | Before W3: `verified:false`; after: `verified:true, credential:"selfie"`; never includes the nullifier | **Before: PASS; after: UNTESTED (needs W3).** 16:13: `{"enabled":true,"purpose":"provider","subject":"0xff21…489b","verified":false,"credential":null,"verifiedAt":null}` — no nullifier field |
| W5 | Provider badge after W3 | `/api/providers` shows the node `humanBacked:true, humanProof:"world-id"`; job board Providers page shows the "human" badge on it | **UNTESTED** — depends on W3. The negative case is verified: unverified node → `humanBacked:false, humanProof:null` and no badge (A16, B3, P7) |
| W6 | Buyer label | A job paid from a World-ID-verified buyer address shows `buyerHumanBacked:true`; a job from an unverified address shows false | **Unverified half PASS; verified half UNTESTED (needs a World App scan for the buyer).** Jobs paid from the unverified demo payer carry `buyerHumanBacked:false` (P2 JSON, A14 "buyer human-backed: not proven") |

## 6. Automated suites (supporting evidence, not a substitute for the above)

| ID | Suite | Correct means | Status |
|---|---|---|---|
| T1 | protocol vitest | all pass | **PASS** — 55/55 (13:47) |
| T2 | app vitest | all pass | **PASS** — 25/25 (13:48) |
| T3 | broker vitest | all pass | **PASS** — 125/125 (15:57, after the RPC-starvation fix: 90 existing + 14 AgentKit + 12 World ID + 9 log index), broker `tsc` build clean; protocol 55/55 and build clean on the same change |
| T4 | cli vitest + `tsc --noEmit` | all pass, no type errors | **PASS** — 130/130 + `tsc` build clean again at 16:08 after the non-TTY dashboard fix (first 130/130 at 14:38). First runs failed for environment reasons (missing `@kazuo/protocol` workspace links after the rename; then 30 s import timeouts under disk load); fixed by restoring the links. Includes 2 new real-WebSocket tests for the reconnect fix |
| T5 | mcp vitest + `tsc --noEmit` | all pass, no type errors | **PASS** — 8/8 + build clean (14:41). **Fixed:** the harness spawned the server with `npx tsx`, whose per-spawn resolution + transpile exceeded the 25 s handshake budget on this disk (0/8, then 5/8, then 7/8). It now spawns the built `dist/index.js` (the shipped artifact), falling back to the workspace `tsx` only when unbuilt. No assertion changed |

## 7. Results log

(Appended as items run. Newest last.)
- 13:53 IST — **Environment:** Cloudflare quick tunnel `https://founder-controlled-appointment-manual.trycloudflare.com` → local broker `:8402`. `NEXT_PUBLIC_KAZUO_BROKER_URL` on both Vercel projects (and `KAZUO_BROKER_URL` on the app, for `/api/pay`) set to the tunnel; production redeploying. Previews are behind Vercel SSO (`all_except_custom_domains`), so production aliases are the surface under test. **Revert when Railway has its operator key:** set both back to `https://broker-production-03b2.up.railway.app` and redeploy, or production dies with this tunnel.
- 13:53 IST — T3–T5 first attempt invalid: the rename left no `node_modules/@kazuo/protocol` workspace links in broker/cli/mcp/app (the pnpm install that recreates them is still crawling), so every import of `@kazuo/protocol` failed. Links recreated exactly as pnpm makes them (`→ ../../../../packages/protocol`); rebuild + suites rerunning.
- 13:56 IST — L5 PASS (broker unreachable). Broker build clean (0 type errors) with workspace links restored; starting broker.
- 14:15 IST — New scope from the user: World ID (Selfie Check) set up like `_references/comitment-issues`. Items W1–W6 will be added once built. Environment facts verified: RP registered (prod + staging), face check enabled, signing key ↔ signer address match.
- 14:15 IST — T3 broker vitest **PASS 104/104** (90 existing + 14 new AgentKit). T4 cli 125/128 and T5 mcp 0/8 were timeouts under heavy disk load (config module import > 30 s; MCP server spawn via `npx tsx` > 25 s), not assertion failures — rerunning unchanged.
- 14:22 IST — World ID broker module built (`services\/broker\/src\/worldid.ts`, routes `\/api\/worldid\/request|verify|status`, SQLite `human_verifications`, provider badge via `registry.markHuman`, buyer label bound to the settled payer). `kazuo verify` written (terminal QR via idkit-core + WASM shim). T3 PASS 116\/116. W3 needs a person to scan with World App.
- 14:26 IST — **Found and fixed (rename misses the regex could not see):** broker startup banner printed `X O R V   B R O K E R` (spaced letters) → now `K A Z U O`; CLI banner `WORDMARK` was block-letter ASCII art spelling XORV → replaced with KAZUO in the same ANSI Shadow style (rebuild running). Repo-wide search for the X glyph row found no other copy.
- 14:26 IST — Open, being investigated: (a) provider node status page showed "reconnecting" while the broker logged "node connected" (spotted in the user's screenshot); (b) MCP suite spawns the server with `npx tsx`, whose cold start exceeds the 25 s handshake budget on this disk — harness defect, fix is to spawn the workspace `tsx` binary directly; (c) `/api/receipts` 502 from Arc RPC rate limiting + a reader that cannot reach entries >360k blocks back (design: persisted forward index).
- 14:26 IST — Broker restarted on the World ID build; job board redeploying with the buyer World ID widget (`components/world-verify.tsx`, `@worldcoin/idkit` 4.2.3).
- 14:30 IST — W1 checked against the running broker with curl (browser check pending): `/api/worldid/status` → `enabled:true, verified:false`; `/api/worldid/request` → `app_id app_d7d26bb5…89e2`, `action kazuo-provider`, lowercased signal, `preset selfieCheckLegacy`, `rp_id rp_30d29cd7ee86bae6`, 65-byte signature; bad subject → 400 `invalid_subject`. CLI banner now reads KAZUO (verified in `kazuo verify --help` output).
- 14:30 IST — **FAIL (P1 area): provider connection state.** After the broker restart, node `/health` reports `connected:false` while broker `/api/providers` reports the same node `connected:true, status:online`. Suspected: a stale socket's `close` handler resetting the flag after the new socket opened. Root-causing in `packages/cli/src/node.ts`.
- 14:41 IST — **Provider connection-state FAIL → fixed** in `packages/cli/src/node.ts`: events from a replaced WebSocket are now ignored, reconnects use the latest control-channel URL, and re-registration goes through `switchControlChannel()` (cancels the stale reconnect, detaches the old socket first). Verified by `test/node-channel.test.ts` over real sockets (switch keeps `connected:true` and never re-dials the old URL; a genuine drop still recovers). Live re-verification on the running node pending a provider restart.
- 14:41 IST — **`/api/receipts` 502 → fix built:** `services/broker/src/log-index.ts` — forward index of KazuoLog persisted in SQLite (`log_entries`, `log_cursor`), seeded from the 5 receipt transactions already in `data/kazuo.db`, paced 1 window / 2.5 s under Arc's measured RPC limit with exponential backoff, following the head once caught up; `/api/receipts` and `/api/log/:kind` serve from it with a `sync` block. Protocol gained `decodeEntryEvents`, `readLogRange`, `readLogFromTransactions` (protocol 55/55 after the refactor). Broker typecheck + suite running.
- 14:52 IST — **Provider connection-state FAIL → re-verified PASS live:** broker restarted on the new build (listening in 18 s); the node's heartbeat got 401, it re-registered (`prv_fUh-sKsJ-GcT`) and both node `/health` and broker `/api/providers` showed connected 22 s later; 10 samples over 30 s: 0 disconnected.
- 14:52 IST — **`/api/receipts` still FAIL (now 200, but 0 receipts):** the index could not seed — Arc's public RPC returns `null` for `eth_getTransactionReceipt` / `eth_getTransactionByHash` of the August transactions (it does not serve historical tx lookups), while ArcScan's API still has them and KazuoLog `count()` = 65 entries. And a design flaw: a seed failure aborted the whole tick, so the forward scan never started (backoff 5→10→20→40 s). Fixing both.
- 15:38 IST — **`/api/receipts` → PASS.** Seeding is now isolated (a failed seed can't stop the scan) and `readLogFromTransactions` skips hashes the RPC can't find instead of throwing. Broker restarted on it: first real receipt (seq 26, block 55,592,567) served 102 s after startup; the index keeps backfilling from the persisted cursor and serves new receipts the moment they're published (seq 66 appeared within seconds of P2).
- 15:50 IST — **FAIL found → fixed: the index starved the broker's own writes of RPC budget.** `/api/network` showed `logPublished.registry 0, heartbeat 0` and `logLastError` = a heartbeat `append` refused with "rate limit exceeded" (at `eth_gasPrice`). Root cause, two parts: (1) the backfill paces at one `eth_getLogs` per 2.5 s — about the whole of Arc's public-RPC budget — with no notion of more important traffic; (2) viem retries -32005 "limit exceeded" by default, but with a 150 ms doubling backoff over three attempts (about 1 s total), far shorter than the limit window, so any write that met a busy moment failed outright. Fix: (a) `packages/protocol/src/chain.ts` — both `readClient` and `writeClient` retry 5× with 1 s doubling (rides out the window; covers the facilitator's settlements, which use the same clients); (b) `Chain.writing()` / `noteWrite()` in `services/broker/src/chain.ts` track in-flight writes (audit publishes, plus settlements via new `onBeforeSettle` / `onSettleFailure` / `onAfterSettle` hooks) with a 5 s cool-down and a 120 s stale guard; (c) `LogIndex` takes `yieldTo` and makes **no** RPC call while the broker is writing (`"yielded"` outcome; new test asserts zero calls while yielding). Also `/api/network` `logLastError` now carries only viem's short message + the RPC reason instead of the full calldata dump. T3 125/125, protocol 55/55. Broker restarted on the build for live re-verification (C3/P1).
- 16:06 IST — **RPC-starvation fix re-verified live:** within 10 s of the restart `/api/network` showed `logPublished {registry 1, heartbeat 1}` and `logLastError null` (was 0/0 plus a rate-limit error). The index kept scanning (57.41M → 57.66M blocks) and held its cursor while P3's settlement and receipt went out (`scannedTo` unchanged between t+80 s and t+90 s) — it yielded, as designed. Registration seq 67 and heartbeat seq 68 are on Arc (C3).
- 16:08 IST — **A20 FAIL found → fixed:** at 375 px the job board's "Recent jobs" section measured 537 px, so the page overflowed and the browser widened the layout viewport to 557 px (which also threw off taps on the drawer). Root cause: `app/page.tsx` used `grid lg:grid-cols-[1.25fr_1fr]` — with no mobile template the single implicit column is `auto`, sized to the min-content of a `truncate` (nowrap) job line. Fix: `grid-cols-1` (= `minmax(0,1fr)`) on mobile and `minmax(0,…)` tracks at `lg`; the same pattern fixed in `components/job-view.tsx`, whose job page has long unbreakable hashes. Landing grids checked and left alone — L8 measured no overflow there. Deployed to production (`dpl_H2YrccYnu3gm32C1LPkcCEG2LCcW`, READY); re-test pending.
- 16:08 IST — **Found in passing → fixed: provider logs flooded off a terminal.** Under a process manager (the preview launcher here) the provider log was nothing but "payout 0xff21… · localhost:8411 · ctrl-c to stop", once a second: `ui.liveRegion()` printed a frame's last line on every repaint when stdout isn't a TTY, and that last line is the static footer. Fix (`packages/cli`): off a TTY `kazuo start` prints the dashboard once and streams each node log entry as a timestamped line; `liveRegion` no longer prints anything off a TTY. T4 130/130, build clean.
- 16:08 IST — **P7 made checkable:** the broker never recorded whether a registering node's AgentKit proof was present or valid, so "no 401" could not distinguish "verified" from "not sent". Register now logs `agentkit proof verified — human-backed in AgentBook | no human in AgentBook`, `no proof sent`, or `agentkit rejected — <reason>`. T3 125/125.
- 16:10 IST — **The 503s in the extension's network list are not the app's requests — evidence:** the page's own Resource Timing (Chrome reports `responseStatus`) listed 130 requests including 17 RSC prefetches, and the only one ≥ 400 was the deliberate A7 `/api/quotes` 503. The `HEAD /` 503s and the occasional `?_rsc=` 503s the extension reported are absent from it; 16 parallel RSC prefetches with the router headers via curl all returned 200; and the page's own `fetch(HEAD)` returns 200.
- 16:24 IST — **Phase 4 regression pass on the final state** (app `dpl_H2YrccYnu3gm32C1LPkcCEG2LCcW`; broker and provider restarted on the last builds, which add close-code logging, the CONNECTING label and non-TTY colour handling; T3 125/125, T4 130/130). Re-run in Chrome, each with 0 failing requests in the page's own resource timing and 0 console errors:
  - **A1** home: composer, 9 jobs, provider Online, "1 provider(s) live".
  - **A16** providers: kazuo-proof-node Online, Echo (test) · Codex, "beat just now", 4 done, no human badge.
  - **A17** network: 7 settled, $0.8550, audit Registry 1 / Heartbeat 1 this session, 9 receipts read from KazuoLog, newest `job_J386WaMK0o0e`.
  - **A14** job page `job_J386WaMK0o0e`: result, log, provider and receipt panels; "View transfer on ArcScan" → `0xbcf23234…0d8b`, "View on-chain receipt" → `0x747fe855…a65b`; grid tracks `minmax(0,…)`.
  - **L1/L4** landing: exact title, document 200, `/api/receipts` + `/api/network` 200, 6 live receipt rows, 0 Xorv/Hedera/HBAR/HashScan mentions.
  Provider log on the new CLI starts with "● CONNECTING … no beat yet" and then streams "ok control channel open".
- **Timing note (added after the fact):** the hand-written times from the 15:38 entry through the 16:24 entry run about one hour ahead of the machine clock — the browser console stamped the A7 run 3:07 PM, logged here as 16:07. The order of events and every result are as recorded; only the clock labels drift. Entries below use the machine clock.
- 15:34–15:45 IST (machine clock) — **The Mac restarted mid-run; production recovered and re-verified.** What the restart broke, and the fix for each:
  - **Quick-tunnel URL changed** (old one returns Cloudflare 530), so both production frontends pointed at a dead broker. New tunnel `https://surround-ports-prime-audience.trycloudflare.com`; `NEXT_PUBLIC_KAZUO_BROKER_URL` (both projects) and `KAZUO_BROKER_URL` (app) updated through the Vercel API and read back; both projects redeployed (landing `kazuo-26unzcc9k`, app `kazuo-arc-lbbdmrhzc`, both READY); the app's production JS contains the new URL and no chunk contains the old one. Railway's domain now answers "Application not found", so the tunnel remains the production path.
  - **Broker and CLI would not start:** `@worldcoin/idkit-core`, `qrcode` and `@types/qrcode` were symlinked from a scratch install in `/private/tmp`, which the reboot cleared. Reinstalled at the pinned versions (4.2.4, ^1.5.4, ^1.5.5) into `node_modules/.kazuo-local-deps` inside the repo, so they survive reboots, and relinked; both packages resolve.
  - **Provider node config lived in the wiped scratchpad:** rebuilt at `~/.kazuo-arc-demo/provider/config.json` (0600) with the same label, region and capabilities; the payout key comes from `.env` at launch via `KAZUO_PRIVATE_KEY` (key ↔ `0xff21…489B` verified before writing), so no secret is in `.claude/launch.json`.
  - **Receipts index resumed from SQLite** (cursor 61,678,361, 62 entries), not from the deployment block.
  - **Re-verified on the recovered stack**, each with 0 failing requests in the page's own resource timing and 0 console errors: **L1/L4** landing (broker calls go to the new host, 200; 6 receipt rows; not offline); **A1** home (9 jobs, provider Online, "1 provider(s) live"); **A16** providers (Online, Echo (test) · Codex, beat 13 s ago); **A17** network (7 settled, $0.8550, 9 chain receipts); **P2-equivalent** paid echo job `job_udztt_-re1Ti` via `kazuo run` (settlement `0x8d77bd8c…c60a`, receipt `0x748f671b…2512`, provider registration tx `0x1d575561…5769`); **A14** its job page links exactly those two txs. **T2** app 25/25, **T5** mcp 8/8 (build clean) — the two suites the restart had interrupted.
- 15:47 IST (machine clock) — **Provider control-channel stability, 8-minute watch on the recovered stack:** node `/health` and broker `/api/providers` sampled every 5 s — 96 samples, 0 not connected, no state change after the first sample, and no close logged on either side (both ends now log the WebSocket close code). The single 1.5 s idle drop seen earlier did not recur; if it does, the logged code will name the cause.

## 8. Run 3 — full plan, top to bottom, after the reboot (16:12–17:15 IST, machine clock)

Environment: broker at `https://surround-ports-prime-audience.trycloudflare.com` (restarted three times on successive fixes), landing and job board on Vercel production (final app deploy `dpl_gCGYazuzC7LeF7eCPrGERmkwxW4Y`), provider `kazuo-proof-node` (echo + codex), demo payer `0x0329…9F36`. Every browser item ran in Claude in Chrome against production; mobile items at 375×812 in the desktop app's Browser pane (Chrome's window here ignores resizing). Console and network were checked on every item; network status comes from each page's own Resource Timing, because the extension's network list shows probe requests the page never makes (see the 16:10 log entry).

**Nine defects found and fixed at the root in this run, plus one gap closed (10); every one re-verified live after its fix:**

| # | Item | What was wrong | Fix |
|---|---|---|---|
| 1 | B22 | `GET /api/worldid/status?subject=abc` answered 200 "not verified" | Route validates the subject; 400 `invalid_subject`, same rule as minting a request (`services/broker/src/app.ts`) |
| 2 | L2 / L10 | Arc sponsor link `circle.com/arc` returned 404; canonical, Open Graph and JSON-LD URLs pointed at `kazuo.network`, which does not resolve | `ARC_URL` → `https://www.arc.io/`; `SITE` → `https://kazuo-arc.vercel.app`; landing redeployed |
| 3 | A21 | Unknown job-board URLs logged two console errors ("Error checking Cross-Origin-Opener-Policy: HTTP error! status: 404") from the Coinbase Wallet SDK inside Privy, whose start-up HEAD request hit the 404 | Pages moved into an `app/(board)` route group that owns the wallet providers and shell; a provider-free `app/not-found.tsx` |
| 4 | A24 | A real job page with the broker unreachable said "Job not found — It may have expired", and after the broker returned it never recovered (an HTTP error closes an `EventSource` for good) | The page tells "unreachable" from "not found"; the stream re-fetches the job and reopens after a closed stream (`app/(board)/jobs/[id]/page.tsx`, `components/job-view.tsx`) |
| 5 | A2 | After a node stopped, "Live providers" listed it as Offline for ten minutes, hiding the empty state while the sidebar said "0 provider(s) live" | Provider lists show only online/busy nodes (`components/live-lists.tsx`) |
| 6 | A22 | A running job's page threw React hydration error #418: its "took" timer was read from `Date.now()` during render | The live timer exists only after mount and ticks each second |
| 7 | B21 | Cancelling a running job: the provider's late "failed" report overwrote "cancelled by the buyer", counted a second failure against the provider and tried to reassign the cancelled job | Results for terminal jobs are ignored; the cancel route publishes the failed receipt itself |
| 8 | P11 | A provider killed mid-job left its paid buyer waiting for the ten-minute job ceiling | A 60 s disconnect grace, then in-flight jobs fail over through the normal path (reassign when another node exists) |
| 9 | P7 | After the reboot the node registered with **no** AgentKit proof and nothing said why: its key came through a launcher step that produced an empty `KAZUO_PRIVATE_KEY`, and the CLI skipped the proof silently | CLI now logs every reason a proof is not sent (`packages/cli/src/agentkit.ts`, `node.ts`); the demo node keeps its key in its 0600 config like `kazuo init` does |
| 10 | A9 / A10 / A18 / A19 | Not a crash but a gap: the no-wallet "Pay and run" worked only with the demo payer's private key copied into Vercel's environment, so four items could not run in a browser at all | New `POST /api/demo/pay` on the broker pays through its own public x402 route with the key it already holds (quote id validated, $0.25 ceiling, 10 requests/min, 501 without a key); `/api/pay` on Vercel relays to it when Vercel has no key. Four new integration tests (broker 129) |

**Final status of every item (Run 3):**

| ID | Status | Evidence in this run |
|---|---|---|
| L1 | PASS | Title exact, document 200, 0 failing requests, 0 console errors (re-run on the final state) |
| L2 | PASS (after fix 2) | Arc → `arc.io`, World AgentKit → docs.world.org, Privy → privy.io, "Post a job" → job board; canonical/OG = kazuo-arc.vercel.app |
| L3 | PASS | Seven real clicks on the header links: each hash set, each section at top 0 |
| L4 | PASS | `/api/receipts` + `/api/network` 200, live receipt rows, contract link |
| L5 | PASS | Broker down: "live feed offline — the contract above is still readable on ArcScan", 2 ArcScan links, no exception |
| L6 | PASS | Real clicks: Q2 closed, Q3 opened (single-open accordion), answer shown |
| L7 | PASS | 0 Xorv/Hedera/HBAR/HashScan; footer World AgentKit + Privy + `eip155:5042002` |
| L8 | PASS | 375 px: no overflow; menu opens; tap Receipts → `#ledger`, menu closes |
| L9 | PASS | All sections have headings and content; adapters list names all five; no undefined/NaN |
| L10 | **FAIL — owner action** | After fix 2 every link answers 200 except two that no code change can fix: `github.com/nickthelegend/kazuo-arc` (404, repository not public) and the npm package page (registry 404, not published) |
| L11 | PASS | 404 with Next's not-found page, no exception |
| A1 | PASS | Composer, jobs, provider Online, "1 provider(s) live", Privy config 200, 0 failing, 0 console errors |
| A2 | PASS (after fix 5) | "No providers online" + hint on home and Providers page. Jobs-empty: a second real broker on a fresh SQLite file (not a deletion of the real jobs) → "No jobs yet — Post one above — it settles on Arc in about a second.", 0 console errors |
| A3 | PASS | Broker down: "broker offline", "Can't reach the broker" ×2, no exception |
| A4 | PASS | Max `0` and `abc` → "Set a maximum price above zero.", 0 `/api/quotes` requests |
| A5 | PASS | Provider down: 503, "no providers are online right now — start one with `kazuo start`" |
| A6 | PASS | 200; body `adapter:"echo"`; card: node, "Echo (test)", pays → provider address, $0.0010 |
| A7 | PASS | Max 0.0005 → 503 "no online provider matches that request under $0.0005" |
| A8 | PASS | Picker lists all seven options; Echo selected → "Model: Echo" |
| A9 | PASS (after change 10) | Real click on "Pay and run" with no wallet → `/jobs/job_AQ8QgUyjAd4v` Completed: payer `0x03294Ce27e…`, paid to `0xff21…489B`, 1000 µUSDC, transfer `0xf3d78946…`, receipt `0x55e41449…`; broker log `POST /api/jobs/qte_thoK_D25A-no → 200`; 0 console errors |
| A10 | PASS (after change 10) | Broker started without a demo key: `/api/pay` relays 501 "No demo payer configured on this broker. Set KAZUO_DEMO_PAYER_KEY — see .env.example.", the page stays on the quote |
| A11 | PASS | Privy modal with email + wallet, app config 200, closes cleanly |
| A12 | UNTESTED | Needs a Privy email login (one-time code to a real inbox) |
| A13 | UNTESTED | Same dependency as A12 |
| A14 | PASS | Completed Codex job page: result, provider/receipt panels, ArcScan transfer + receipt links, sha256 |
| A15 | PASS | "Job not found" for an id that never existed, 200, no exception |
| A16 | PASS | Node Online, capabilities, address, heartbeat, no human badge |
| A17 | PASS | Network facts, audit counts, 22 receipts read from KazuoLog |
| A18 | PASS (after change 10) | Browser: a quote issued by a broker that then went away, paid from the page → 404 "quote not found or expired — request a new one" shown on the quote card, no navigation, no exception. Broker: same 404 (B5) |
| A19 | PASS (after change 10) | Browser: Back from the paid job no longer offers the quote card; the page's own `POST /api/pay` for the paid `qte_thoK_D25A-no` → 409 "this quote has already been paid"; `fetch` native, 0 failing resources, 0 console errors on a fresh load. Broker: 409 with and without a payment header |
| A20 | PASS | 375 px with 16 jobs: no overflow; drawer opens; tap Providers routes and closes it |
| A21 | PASS (after fix 3) | 404 "This page doesn't exist", no Privy scripts, 0 console errors |
| A22 | PASS (after fix 6) | Page opened on a Running Codex job; timer ticked 6.8 s → 9.8 s; same document turned Completed with answer and receipt; `/stream` 200 in the broker log; 0 console errors |
| A23 | PASS | Broker stopped and restarted with the page open: offline → "1 provider(s) live", 15 jobs, same document |
| A24 | PASS (after fix 4) | Broker down: "Can't reach the broker — The job may still exist…"; broker back: same document loaded the Completed job |
| B1–B12, B14–B20 | PASS | 23/23 API checks on the final broker build (health, network, providers without secrets, quote validation, 402 header + CORS, AgentKit challenge + garbage header, metrics, receipts, human-backed-only, job list/detail/stream, log streams, register validation, heartbeat and callback auth) |
| B13 | PASS | Requests 1–30 → 200, 31st → 429 "rate limit exceeded — 30 requests per 60s" |
| B21 | PASS (after fix 7) | Running Codex job cancelled → 200 `{status:"failed", refunded:false}`; error stays "cancelled by the buyer"; failed receipt `0xd81bab80…`; second cancel 409 |
| B22 | PASS (after fix 1) | `abc` → 400 `invalid_subject`; valid address → 200 |
| C1 | PASS | Codex settlement `0x18e180cd…`: one Transfer payer → provider 200000, sent by the operator |
| C2 | PASS | Receipt seq 84: jobId, resultHash and settlement match |
| C3 | PASS | Current node's registration entry seq 81, echo + codex |
| C4 | PASS | AgentBook 3,569 bytes on World Chain (chain 480) |
| C5 | UNTESTED | No ETH on World Chain Sepolia; faucets need a human |
| C6 | PASS | Two real nodes: original killed mid-job → "reassigned to kazuo-proof-node-b at no extra charge" → completed; one settlement `0xc95a7db3…`, receipt `0xf0b578d0…` |
| N1 | PASS | `/health` connected, `/info` without key material, status page "connected to the network" |
| P1 | PASS | Restarted node Online on `/providers` within one heartbeat |
| P2 | PASS | Echo job `job_WllqrCDkkGs9` completed and settled |
| P3 | PASS | Codex job `job_XkZRVWOq3s2o`, $0.20, model-written answer |
| P4 | PASS | MCP: five tools, `kazuo_run_job` paid with transaction and receipt |
| P5 | PASS | M1 PASSED, buyer gas 0 wei |
| P6 | PASS | "not registered" in AgentBook |
| P7 | PASS (after fix 9) | "agentkit proof verified — no human in AgentBook" on first registration and on re-registration after two broker restarts |
| P8 | PASS | doctor "nothing broken"; status/earnings/jobs/wallet/price/logs/completion exit 0; test echo healthy; pause/resume; config redacts the key |
| P9 | PASS | The `/kazuo` skill's exact command completed through the public broker |
| P10 | PASS | `--human-backed-only` refused before paying |
| P11 | PASS (after fix 8) | Provider killed mid-job → buyer got "failed — provider disconnected mid-job" 61 s later; failed receipt published; restarted node re-registered |
| W1 | PASS | RP signature recovers to `0xd4F041AB…9b78Dc`; actions, signal, preset; bad subject 400 |
| W2 | PASS | Unknown nonce 403 before World is called; malformed 400 |
| W3 | UNTESTED | Needs a person to scan with World App |
| W4 | Before PASS; after UNTESTED | `verified:false`, no nullifier; the "after" half needs W3 |
| W5 | UNTESTED | Depends on W3 |
| W6 | Unverified half PASS; verified half UNTESTED | Unverified payer → `buyerHumanBacked:false`; the verified half needs a World App scan |
| T1–T5 | PASS | protocol 55, broker 125, cli 130, mcp 8, app 25; `tsc` clean |
